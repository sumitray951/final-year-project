# TODO

- [x] Ensure `models/legalbert-risk/` directory is created automatically on app startup (even if model weights are missing)

- [ ] Keep inference fallback behavior unchanged when model weights are not present
- [ ] Sanity check: run `streamlit run app.py` and `python -m src.train --help` (no crash)

